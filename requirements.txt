# Core runtime dependencies (exact pins; versions verified working with the suite)
reportlab==4.4.2        # synthetic PDF generation
jsonschema==4.23.0      # line-item schema validation

# Test dependencies
pytest==8.4.0

# --- Optional: "real" OCR backend ---------------------------------
# The pipeline ships with a deterministic SyntheticOCREngine so the
# whole thing runs with zero system dependencies. To exercise the
# real Tesseract path instead, install these (pinned) AND the system
# packages `tesseract` and `poppler`:
#
#   pytesseract==0.3.13
#   pdf2image==1.17.0
#   Pillow==11.0.0
