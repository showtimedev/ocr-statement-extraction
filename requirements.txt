# Core runtime dependencies
reportlab>=4.0        # synthetic PDF generation
jsonschema>=4.0       # line-item schema validation

# Test dependencies
pytest>=7.0

# --- Optional: "real" OCR backend ---------------------------------
# The pipeline ships with a deterministic SyntheticOCREngine so the
# whole thing runs with zero system dependencies. To exercise the
# real Tesseract path instead, install these AND the system packages
# `tesseract` and `poppler`:
#
#   pytesseract>=0.3.10
#   pdf2image>=1.16
#   Pillow>=10.0
